{{- if .Values.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: am-routing
  namespace: {{ .Values.alerting.namespace | default "monitoring" }}
  labels:
{{- range $k, $v := .Values.alerting.selectorLabels }}
    {{ $k }}: {{ $v | quote }}
{{- end }}
spec:
  route:
    receiver: {{ .Values.alerting.routes.rootReceiver | quote }}
    groupBy:
{{- range .Values.alerting.routes.groupBy }}
      - {{ . | quote }}
{{- end }}
    groupWait: {{ .Values.alerting.routes.groupWait | default "30s" | quote }}
    groupInterval: {{ .Values.alerting.routes.groupInterval | default "5m" | quote }}
    repeatInterval: {{ .Values.alerting.routes.repeatInterval | default "12h" | quote }}

    routes:
{{- range .Values.alerting.routes.rules }}
      - receiver: {{ .receiver | quote }}
        matchers:
{{- range .matchers }}
          - name: {{ .name | quote }}
            value: {{ .value | quote }}
            matchType: {{ .matchType | default "=" | quote }}
{{- end }}
{{- end }}

  receivers:
    - name: "null"
{{- range .Values.alerting.receivers }}
    - name: {{ .name | quote }}
{{- if eq .type "slack" }}
      slackConfigs:
        - sendResolved: {{ .sendResolved | default true }}
          apiURL:
            name: {{ .apiURLSecretRef.name | quote }}
            key: {{ .apiURLSecretRef.key | quote }}
{{- if .channel }}
          channel: {{ .channel | quote }}
{{- else if $.Values.alerting.slack.channel }}
          channel: {{ $.Values.alerting.slack.channel | quote }}
{{- end }}
{{- if .titleTemplate }}
          title: '{{`{{ template "`}}{{ .titleTemplate }}{{`" . }}`}}'
{{- end }}
{{- if .textTemplate }}
          text: '{{`{{ template "`}}{{ .textTemplate }}{{`" . }}`}}'
{{- end }}
{{- end }}
{{- else }}
      # receiver type not implemented: {{ .type }}
{{- end }}
{{- end }}
{{- end }}
