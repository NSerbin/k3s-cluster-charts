replicaCount: 1

fullnameOverride: grafana-mcp

image:
  repository: grafana/mcp-grafana
  tag: "1.1.0"
  pullPolicy: IfNotPresent

grafana:
  url: "http://kube-prometheus-stack-grafana.monitoring.svc.cluster.local"
  secretName: grafana-mcp-secrets
  tokenKey: GRAFANA_SERVICE_ACCOUNT_TOKEN

mcp:
  port: 8000
  transport: streamable-http
  endpointPath: /mcp
  logLevel: info
  allowedHosts:
    - grafana-mcp
    - grafana-mcp:8000
    - grafana-mcp.monitoring
    - grafana-mcp.monitoring:8000
    - grafana-mcp.monitoring.svc
    - grafana-mcp.monitoring.svc:8000
    - grafana-mcp.monitoring.svc.cluster.local
    - grafana-mcp.monitoring.svc.cluster.local:8000
  disabledFlags:
    - --disable-write
    - --disable-admin
    - --disable-api
    - --disable-asserts
    - --disable-incident
    - --disable-oncall
    - --disable-plugin
    - --disable-proxied
    - --disable-pyroscope
    - --disable-rendering
    - --disable-sift
    - --disable-snapshot
    - --disable-annotations
    - --disable-provisioning

service:
  type: ClusterIP
  port: 8000

resources:
  requests:
    cpu: 25m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 999
  fsGroup: 999
  fsGroupChangePolicy: OnRootMismatch
  seccompProfile:
    type: RuntimeDefault

securityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 999
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

nodeSelector: {}
tolerations: []
affinity: {}
